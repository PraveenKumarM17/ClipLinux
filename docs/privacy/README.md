# Privacy notes

Canonical document: [/PRIVACY_MODEL.md](../../PRIVACY_MODEL.md)

Implementation: `crates/unipick-privacy`. History integration:
`crates/unipick-clipboard` (`record()` applies `decide()` before storage).
