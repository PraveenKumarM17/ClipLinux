# Themes

Status: **Planned / Not implemented**

JSON design tokens intended for the Svelte UI. Themes must not include
executable code.

`default-dark.json` is a token sketch. The desktop shell does **not** load
this file yet; colors live in `apps/desktop/src/app.css`.

Do not call theming “supported”.
