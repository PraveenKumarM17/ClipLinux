# Local sticker packs

Status: **Planned / Not implemented**

Drop community or personal sticker packs here. Each pack is a directory with a
`pack.json` and image files.

No packs are shipped. `clipl-media::LocalStickerLibrary` returns an empty list
until the scanner is implemented. `manifest.json` is an empty registry.

Do not call stickers “supported”.
