# Local sticker packs

Drop community or personal sticker packs here. Each pack is a directory with a
`pack.json` and image files.

No packs are shipped in the foundation. `unipick-media::LocalStickerLibrary`
returns an empty list until the scanner is implemented.
